# Traffic Flow fixture

`traffic-flow-anonymized.pbf` is a small MVT fixture for the `Traffic flow`
layer. Its coordinates do not represent the source location, and it contains no
provider identifiers. It preserves representative Traffic Flow properties and
covers full and one-side road coverage, closures, multipart lines, and absent
speed metadata.
